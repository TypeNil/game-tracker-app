package io.github.typenil.gametracker.feature.settings

import android.content.Context

internal object DebugNotificationActions {
    const val isVisible: Boolean = false

    fun send(context: Context) = Unit
}
