package io.github.typenil.gametracker.core.model

/**
 * Categorization for release notification event types.
 */
enum class NotificationEventType {
    RELEASE_TODAY,
    RELEASE_SOON,
    DATE_CHANGED
}
