ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.gametracker.backend.ApplicationKt.module ]
    }
}

igdb {
    clientId = ""
    clientId = ${?IGDB_CLIENT_ID}
    clientSecret = ""
    clientSecret = ${?IGDB_CLIENT_SECRET}
}
