ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ io.github.typenil.gametracker.backend.ApplicationKt.module ]
    }
}

igdb {
    clientId = ""
    clientId = ${?IGDB_CLIENT_ID}
    clientSecret = ""
    clientSecret = ${?IGDB_CLIENT_SECRET}
}
